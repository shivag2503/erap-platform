name: CI - Payment Service

on:
  push:
    paths: ['erap-payment-service/**']
  pull_request:
    paths: ['erap-payment-service/**']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder - no code yet
        run: echo "Payment Service CI - tests will be added when service is built"