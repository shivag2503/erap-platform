name: CI - Order Service

on:
  push:
    paths: ['erap-order-service/**']
  pull_request:
    paths: ['erap-order-service/**']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder - no code yet
        run: echo "Order Service CI - tests will be added when service is built"