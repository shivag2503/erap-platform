name: CI - Product Service

on:
  push:
    paths: ['erap-product-service/**']
  pull_request:
    paths: ['erap-product-service/**']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder - no code yet
        run: echo "Product Service CI - tests will be added when service is built"